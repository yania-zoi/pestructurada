#include <stdio.h>
#include <string.h>

int main(){

int tablero[5][5];
FILE *archivo;
archivo = fopen("tablero.txt", "w");
fprintf(archivo, "yania w\n");

for(int i = 0; i < 5; i++){
    for(int j = 0; j < 5; j++){
    tablero[i][j] = '~';
    }
}

tablero[1][2] = 'B';
tablero[3][4] = 'x';
tablero[2][1] = 'O';

for(int i = 0; i < 5; i++){
    for( int j = 0; j < 5; j++){
        printf("%c", tablero[i][j]);
    }
    printf("\n");
}

for(int i = 0; i < 5; i++){
    for( int j = 0; j < 5; j++){

        fprintf(archivo,"%c", tablero[i][j]);

    }

    fprintf(archivo, "\n");
}
fprintf(archivo, "yania w");

fclose(archivo);


return 0;
}
